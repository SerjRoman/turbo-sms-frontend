import { Link } from "expo-router";
import { Text, View } from "react-native";

export default function Chats() {
	return (
		<View style={{ flex: 1 }}>
			<Link href={"/settings"}>
				<Text>TEXXTTXXTXTXT</Text>
			</Link>
		</View>
	);
}
